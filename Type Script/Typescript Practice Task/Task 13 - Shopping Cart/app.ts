const shoppingCart: {
  productName: string;
  price: number;
  quantity: number;
} = {
  productName: "Laptop",
  price: 45000,
  quantity: 2,
};

console.log(`Product: ${shoppingCart.productName}`);
console.log(`Price: ${shoppingCart.price}`);
console.log(`Quantity: ${shoppingCart.quantity}`);
