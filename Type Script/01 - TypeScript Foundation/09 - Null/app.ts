// null represents an intentional absence of a value.

let selectedUser: null = null;

let rightNowMyPocketMoney: null = null;

let mySemesterFinalCgpa: null = null;

console.log(selectedUser);
console.log(rightNowMyPocketMoney);
console.log(mySemesterFinalCgpa);
