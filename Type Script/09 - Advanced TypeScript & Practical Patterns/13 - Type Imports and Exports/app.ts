// app.ts

import type { User, UserRole } from "./user";

const user: User = {
  id: 1,
  name: "Foysal",
  email: "foysal@example.com",
};

const role: UserRole = "admin";

console.log(user);
console.log(role);
